version: '3.8'

services:
  postgres:
    image: postgres:18-alpine
    container_name: mealflip_postgres
    environment:
      POSTGRES_DB: mealflip_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d mealflip_dev"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
